{% extends 'base.html.twig' %}

{% block title %}Manga{% endblock %}

{% block body %}
    <h1>Manga</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ manga.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ manga.Nom }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ manga.Image }}</td>
            </tr>
            <tr>
                <th>NomAuth</th>
                <td>{{ manga.nomDuCreateur }}</td>
            </tr>
            <tr>
                <th>Dessin</th>
                <td>{{ manga.Dessin }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ manga.Genre }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ manga.Statut }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('manga_index') }}">back to list</a>

    <a href="{{ path('manga_edit', {'id': manga.id}) }}">edit</a>

    {{ include('manga/_delete_form.html.twig') }}
{% endblock %}
