{% extends "base.html.twig" %}

{% block body %}
    <div class="container py-5">
        <div class="row">
        
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h3>Modifiez votre pseudo</h3>
                    </div>

                        {{ form_start(form) }}
            
                            {{ form_row(form.plainPseudo.first) }}

                            {{ form_row(form.plainPseudo.second) }}
                       
                            <button type="submit" class="btn btn-outline-info mt-3">Validez</button>
                            <a href="{{ path('profile') }}" class="btn btn-outline-danger mt-3">Annulez</a>

                        {{ form_end(form) }}

                    </div>
                </div>
            </div>
        </div>
    </div>


{% endblock %}
