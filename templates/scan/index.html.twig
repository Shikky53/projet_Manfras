{% extends 'base.html.twig' %}

{% block title %}Manga index{% endblock %}

{% block body %}
            <h1 class="px-3">Les pages</h1>
            <div class="container px-3 py-4">
                <a href="{{ path('scan_new') }}" class="px-5 btn btn-outline-info">Ajoutez une page</a>
            </div>
            
            <div class="container py-3">
                <div class="row justify-content-evenly">

        {% for scan in scans %}

                    <div class="card border-light col-3">
                        <div class="card-body pt-2">
                            <div class="card-title pl-4 pt-3">
                                <h4> Page N°{{scan.numero}}</h4>
                            </div>
                            <img class="img-fluid" src="{{ scan.image }}" alt="">
                        </div>
                        <div class="pb-4 text-center">

                        </div>
                        <div class="card-text text-center d-flex justify-content-around mb-3">
                            <a href="{{ path('scan_edit', {'id': scan.id}) }}" class="btn btn-outline-info">Modifier</a>
                            <a href="{{ path('scan_delete', {'id': scan.id}) }}" class="btn btn-outline-danger">Supprimer</a>
                        </div>
                    </div>
        
        {% endfor %}

                </div>
            </div>

{% endblock %}
