{% extends "base.html.twig" %}

{% block body %}

<div class="container py-5">

    <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h5>Prendre contact avec nous</h5>
                    </div>

                    <div class="card-body">
                        {{ form_start(form) }}

                        {{form_widget(form) }}

                        <button class="btn btn-outline-info mt-3">Envoyer</button>
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
    </div>

</div>

{% endblock %}